# 小红书笔记检测 Skill 增强计划 v2.2

## 目标

基于用户提供的修改意见，对 SKILL.md 进行系统性增强，提升执行约束、检测能力和输出质量。

## 当前状态分析

SKILL.md 已完成 v2.1.0 架构重构：
- 采用调度中心模式，四大核心工作流
- 知识库按需加载（@读取 机制）
- 标准化 9 步输出结构
- 缺少：执行约束、唤醒语、降级策略、自适应输出等

## 修改范围

仅修改 `/Users/roylee/Desktop/Trae/RedBook-Skills/xiaohongshu-content-checker/SKILL.md` 文件。

---

## P0（必须修复）

### 1. 增加强制性执行约束

**位置**：在"核心原则"部分之后，"四大核心工作流"之前

**新增内容**：

```markdown
## ⚠️ CRITICAL 执行规则

- MUST 严格按照"标准化 9 步结构"顺序输出，NEVER 增加额外步骤或调整顺序
- MUST 在执行工作流 N 之前，先通过 `@读取` 加载对应的依赖知识库
- MUST 在给出敏感词/违规判定时，引用具体的知识库分类名称
- NEVER 在未加载知识库的情况下凭"常识"进行判定
- NEVER 自行添加未在输出模板中定义的分析维度或评分项
- NEVER 编造用户未提供的账号信息，缺失信息 MUST 标注为"未提供"或"基于文本推测"
```

### 2. 补充唤醒语/触发词列表

**位置 1**：YAML Frontmatter 的 description 字段

**修改前**：
```yaml
description: 小红书笔记文案敏感词检测与内容定位分析工具。检测敏感词、违禁词、极限用语，分析账号定位与内容匹配度，识别影响自然流的因素，提供针对性的优化建议。当用户需要检查小红书笔记合规性、避免限流、优化内容定位、提升自然流量时使用。
```

**修改后**：
```yaml
description: >-
  小红书笔记文案敏感词检测与内容定位分析工具。检测敏感词、违禁词、极限用语，
  分析账号定位与内容匹配度，识别影响自然流的因素，提供针对性的优化建议。
  当用户需要检查小红书笔记合规性、避免限流、优化内容定位、提升自然流量时使用。
  唤醒语：小红书笔记检测、笔记敏感词排查、小红书文案审核、笔记合规检查、
  内容风控检测、笔记限流诊断、小红书发布前检查、笔记优化建议。
```

**位置 2**：在"概述"部分开头增加唤醒语段落

**新增内容**：
```markdown
## 唤醒语/触发词

当用户输入以下任一关键词时，激活本 Skill：
- 小红书笔记检测 / 笔记敏感词排查 / 小红书文案审核
- 笔记合规检查 / 内容风控检测 / 笔记限流诊断
- 小红书发布前检查 / 笔记优化建议
```

---

## P1（强烈建议）

### 3. 补充批量检测模式的完整逻辑

**位置**：在"使用方法"下的"批量检测模式"部分

**修改前**：
```markdown
### 批量检测模式

```
请批量检测以下笔记（提供账号定位信息后，列出多条笔记内容）
```
```

**修改后**：
```markdown
### 批量检测模式

当用户提供多条笔记内容时：

1. MUST 先完成账号定位确认（仅需一次）
2. 对每条笔记依次执行工作流 2-4
3. 最终输出汇总报告，包含：
   - 各笔记风险等级对比表
   - 共性问题总结
   - 优先修改建议（按风险等级排序）
4. 单条笔记的详细检测报告 MUST 以折叠/引用形式附在汇总报告之后

```
请批量检测以下笔记（提供账号定位信息后，列出多条笔记内容）
```
```

### 4. 增加知识库加载失败的降级策略

**位置**：在每个工作流的"依赖知识库"标注后

**@工作流 2 修改**：
```markdown
**依赖知识库**：`references/detection/SENSITIVE_WORDS.md`、`references/platform/PLATFORM_RULES.md`

> ⚠️ 如果知识库文件加载失败，MUST 使用以下内置基础规则继续执行：
> - 广告法极限词：最、第一、唯一、首个、顶级、极致、100%、永久等
> - 平台导流词：微信、VX、加我、私聊、主页有、评论区见等
> - 并在输出中标注："⚠️ 知识库加载不完整，以下检测基于内置基础规则，建议补充知识库后重新检测"
```

**@工作流 3 修改**：
```markdown
**依赖知识库**：`references/platform/PLATFORM_RULES.md`

> ⚠️ 如果知识库文件加载失败，MUST 使用以下内置基础规则继续执行：
> - 匹配度：检查笔记主题是否与账号领域一致
> - 营销浓度：检查是否出现品牌名堆砌、价格频繁提及
> - 导流风险：检查是否包含微信号、二维码、外部链接
> - 并在输出中标注："⚠️ 知识库加载不完整，以下检测基于内置基础规则，建议补充知识库后重新检测"
```

**@工作流 4 修改**：
```markdown
**依赖知识库**：`references/detection/REPLACEMENT_SUGGESTIONS.md`、`references/examples/CONTENT_EXAMPLES.md`

> ⚠️ 如果知识库文件加载失败，MUST 使用以下内置基础规则继续执行：
> - 绝对表达 → 相对表达（"最好"→"很喜欢的"）
> - 承诺表达 → 体验表达（"保证变白"→"用了一段时间感觉有提亮"）
> - 营销表达 → 场景表达（"赶紧下单"→"有需要的姐妹可以参考"）
> - 并在输出中标注："⚠️ 知识库加载不完整，以下检测基于内置基础规则，建议补充知识库后重新检测"
```

### 5. 增加输出长度自适应策略

**位置**：在"风险等级定义"之后，"注意事项"之前

**新增内容**：
```markdown
## 输出长度自适应规则

根据风险等级动态调整输出详细程度：

| 风险等级 | 分值 | 输出步骤 | 说明 |
|----------|------|----------|------|
| 🟢 安全 | 0 | 步骤 1/2/9 | 总体结论 + 定位卡 + 检查清单 |
| 🟡 低风险 | 1-3 | 步骤 1-5 + 9 | 增加敏感词检测和匹配度分析 |
| 🟠 中风险 | 4-6 | 步骤 1-8 + 9 | 完整输出所有分析维度 |
| 🔴 高风险 | 7+ | 步骤 1-8 + 9 | 完整输出，并在开头增加紧急警告 |

**紧急警告格式（🔴 高风险时）**：
```
🚨 紧急警告：本笔记存在严重违规风险，建议立即修改后再发布！
```
```

---

## P2（建议优化）

### 6. 增加